# GB29 Railway Bot

Upload all files in this folder directly into the root of your GitHub repository.

Railway variables:

```
BOT_SECRET=test123
EXECUTION_MODE=PAPER
ALLOW_LIVE=false
MAX_POSITION=1
DUPLICATE_TTL_SECONDS=60
```

Health check:

```
https://YOUR-RAILWAY-DOMAIN/health
```

Webhook path:

```
https://YOUR-RAILWAY-DOMAIN/webhook
```
